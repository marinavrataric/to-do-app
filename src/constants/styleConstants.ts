import styled from "styled-components";

export const colors = {
  darkBlue: "#36475B",
  blue: "#0081A1",
};

export const CenterDiv = styled.div`
  width: 320px;
  height: 400px;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  margin: auto;
  max-width: 100%;
  max-height: 100%;
  overflow: auto;
  position: absolute;
  text-align: center;
`;
